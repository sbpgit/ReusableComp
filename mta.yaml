_schema-version: "3.2"
ID: reusereusablecomp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: reusereusablecomp-destination-content
  type: com.sap.application.content
  requires:
  - name: reusereusablecomp-destination-service
    parameters:
      content-target: true
  - name: reusereusablecomp-repo-host
    parameters:
      service-key:
        name: reusereusablecomp-repo-host-key
  - name: reusereusablecomp-uaa
    parameters:
      service-key:
        name: reusereusablecomp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: reusereusablecomp_html_repo_host
          ServiceInstanceName: reusereusablecomp-html5-srv
          ServiceKeyName: reusereusablecomp-repo-host-key
          sap.cloud.service: reusereusablecomp
        - Authentication: OAuth2UserTokenExchange
          Name: reusereusablecomp_uaa
          ServiceInstanceName: reusereusablecomp-xsuaa-srv
          ServiceKeyName: reusereusablecomp-uaa-key
          sap.cloud.service: reusereusablecomp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: reusereusablecomp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: reusereusablecomp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - reusereusablecomp.zip
      name: reusereusablecomp
      target-path: resources/
- name: reusereusablecomp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: reusereusablecomp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: reusereusablecomp-destination-service
    service-plan: lite
- name: reusereusablecomp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: reusereusablecomp-xsuaa-srv
    service-plan: application
- name: reusereusablecomp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: reusereusablecomp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true

